// next.config.js
/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: [],
  },
  // Enable experimental appDir if needed (Next 14 has it by default)
};
module.exports = nextConfig;
